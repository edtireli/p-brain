# Core

The framework primitives every stage and plug-in is built on: the run
`Config`, the `Pipeline` orchestrator, the `Stage`/`Manifest` interface,
the universal `Plugin` contract, and the `discover()` auto-discovery
helper.

## Plugin contract

::: pbrain.core.plugin

## Configuration

::: pbrain.core.config

## Pipeline orchestrator

::: pbrain.core.pipeline

## Stage interface

::: pbrain.core.stage

## Manifests

::: pbrain.core.manifest

## Auto-discovery

::: pbrain.core.discovery
