# Pipeline stages

The built-in stages of the default pipeline. The orchestrator wires them
by their `requires`/`name` declarations and connects them through
`manifest.json` files; see [Architecture](../architecture-overview.md).

## Built-in stages

::: pbrain.stages._builtin

## Diagnostics stage

::: pbrain.stages._diagnostics
