"""Synthetic-phantom demo data + output-map montage generator.

Run via ``python -m pbrain.demo``. Creates a small synthetic subject
with known kinetics, executes the full pipeline on it, and saves
parameter-map montages to ``demo/`` for inclusion in the README.

No patient data is involved — the phantom is fully analytical.
"""
