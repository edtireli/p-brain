# p-brain Windows CLI — Python dependencies
#
# Install with:
#   pip install -r windows/requirements.txt
#
# SynthSeg must be installed separately:
#   git clone https://github.com/BBillot/SynthSeg.git vendor/SynthSeg
#   # Download models per SynthSeg README

# Core scientific
numpy>=1.20
scipy>=1.7
nibabel>=3.2
matplotlib>=3.4
scikit-image>=0.19

# SynthSeg's TensorFlow dependency
tensorflow>=2.4
keras>=2.4

# p-brain's own dependencies
tqdm
